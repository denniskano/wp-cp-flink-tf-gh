statement-name: "create-demo-table"
flink-compute-pool: "CP_AZC_${environment}_PEVE_03"

statement: |
  CREATE TABLE  IF NOT EXISTS `${catalog_name}`.`${cluster_name}`.`demo_table` (
    id STRING,
    name STRING,
    created_at TIMESTAMP(3)
  ) WITH (
    'connector' = 'confluent'
  );
statement-name: "insert-input-loop-from-internal-v1"
flink-compute-pool: "CP_AZC_${environment}_PEVE_02"
stopped: "true" # true to pause, false to resume
statement: |
  EXECUTE STATEMENT SET
  BEGIN
    insert into `${catalog_name}`.`${cluster_name}`.`azc-peve-terraform-input-loop` SELECT new_field1, new_field2, new_field3 FROM `${catalog_name}`.`${cluster_name}`.`azc-peve-terraform-internal`;
    insert into `${catalog_name}`.`${cluster_name}`.`azc-peve-terraform-input-loop` SELECT new_field1, new_field2, new_field3 FROM `${catalog_name}`.`${cluster_name}`.`azc-peve-terraform-internal`;
    insert into `${catalog_name}`.`${cluster_name}`.`azc-peve-terraform-input-loop` SELECT new_field1, new_field2, new_field3 FROM `${catalog_name}`.`${cluster_name}`.`azc-peve-terraform-internal`;
  END;
#FIN
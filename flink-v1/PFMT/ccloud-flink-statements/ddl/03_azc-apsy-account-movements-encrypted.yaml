statement-name: "add-virtual-headers-azc-apsy-account-movements-encrypted"
flink-compute-pool: "CP_AZC_${environment}_PFMT_01"
stopped: "false"   #true to pause, false to resume
statement: |
  ALTER TABLE `${catalog_name}`.`${cluster_name}`.`azc-apsy-account-movements-encrypted`
  ADD `headers` MAP<BYTES, BYTES> METADATA VIRTUAL;
###FIN###

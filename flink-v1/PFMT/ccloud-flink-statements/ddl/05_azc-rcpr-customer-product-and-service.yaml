statement-name: "add-virtual-headers-azc-rcpr-customer-product-and-service"
flink-compute-pool: "CP_AZC_${environment}_PFMT_01"
stopped: "false"   #true to pause, false to resume
statement: |
  ALTER TABLE `${catalog_name}`.`${cluster_name}`.`azc-rcpr-customer-product-and-service`
  ADD `headers` MAP<BYTES, BYTES> METADATA VIRTUAL;
###FIN###

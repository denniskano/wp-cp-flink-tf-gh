statement-name: "add-virtual-headers-azc-apsy-account-batch-movements"
flink-compute-pool: "CP_AZC_${environment}_PFMT_01"
stopped: "false"   #true to pause, false to resume
statement: |
  ALTER TABLE `${catalog_name}`.`${cluster_name}`.`azc-apsy-account-batch-movements`
  ADD `headers` MAP<BYTES, BYTES> METADATA VIRTUAL;
###FIN###

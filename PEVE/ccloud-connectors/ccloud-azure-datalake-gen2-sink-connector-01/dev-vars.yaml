# =============================================================================
# CONNECTOR VARIABLES - DEVELOPMENT
# =============================================================================
# Aplicación: PEVE
# Entorno: Development (DES)
# Última actualización: 2024-01-XX
# Responsable: [Nombre del equipo]

# =============================================================================
# CONNECTOR CONFIGURATION (Non-Sensitive) - Environment Specific
# =============================================================================
config_nonsensitive:
  azure.blob.storage.account.name: "dev-storage-account"
  azure.blob.storage.container.name: "dev-container"
  azure.blob.storage.directory: "dev-directory"
  topics: "dev-topic-name"
  format.output.fields.value.name: "value"

# =============================================================================
# CONNECTOR CONFIGURATION (Sensitive) - Environment Specific
# =============================================================================
config_sensitive:
  azure.blob.storage.account.key: ""  # Se obtiene desde Vault/secrets

# =============================================================================
# CONNECTOR STATUS
# =============================================================================
status: "RUNNING"

# =============================================================================
# METADATA (Opcional - para referencia)
# =============================================================================
project: "PEVE"
recipients: dennispaitan@bcp.com.pe
hosted: ccloud
api-key-sequence: "01"


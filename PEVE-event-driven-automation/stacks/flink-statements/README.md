# Stack: flink-statements

Esqueleto. Enlazar `modules/ccloud-flink-statements`. State por pipeline.

Si el SQL usa UDF o connection, aplicar antes `flink-artifacts` / `flink-connections` (y el compute pool).

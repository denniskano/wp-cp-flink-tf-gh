# ccloud-flink-statements

Esqueleto. Migrar desde `flink-v1/terraform/ccloud-flink-statements`.

No sube UDFs ni crea connections: consume ids/nombres de `ccloud-flink-artifacts` y `ccloud-flink-connection`. Pool: `ccloud-flink-compute-pool`.

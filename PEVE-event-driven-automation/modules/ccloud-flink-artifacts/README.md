# ccloud-flink-artifacts

Pendiente. `confluent_flink_artifact` (JAR o Python). Región la misma del compute pool (AZURE / eastus2). El archivo no vive en este repo; el workflow pasa `artifact_file`. El statement usa el id `lfa-…`.

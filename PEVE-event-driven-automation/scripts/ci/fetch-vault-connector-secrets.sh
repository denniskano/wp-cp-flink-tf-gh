#!/usr/bin/env bash
# Placeholder: el fetch de vault.secrets vive hoy en el workflow de Connect.
# Cuando se extraiga de GHA, este script recibe VAULT_ADDR + VAULT_TOKEN y
# escribe CONNECTOR_SECRETS (JSON) a GITHUB_ENV.
set -euo pipefail
echo "fetch-vault-connector-secrets.sh: no implementado; usar el step del workflow."
exit 0
